name: Trigger auto deployment for cntxcontainerapps-api-01ee114b-2

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/cntxcontainerapps-api-01ee114b-2-AutoDeployTrigger-3d8698d4-125c-4bdd-be90-4eb2e626ad8c.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CNTXCONTAINERAPPSAPI01EE114B2_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CNTXCONTAINERAPPSAPI01EE114B2_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CNTXCONTAINERAPPSAPI01EE114B2_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/ui
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: cntxacr.azurecr.io
          registryUsername: ${{ secrets.CNTXCONTAINERAPPSAPI01EE114B2_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CNTXCONTAINERAPPSAPI01EE114B2_REGISTRY_PASSWORD }}
          containerAppName: cntxcontainerapps-api-01ee114b-2
          resourceGroup: cntxcontainerapps-rg-01ee114b
          imageToBuild: cntxacr.azurecr.io/cntxcontainerapps-api-01ee114b-2:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


